{
    "name": "smart-dept-system-root",
    "version": "1.0.0",
    "scripts": {
        "prebuild": "cd backend && npm install && cd ../frontend && npm install",
        "build": "cd frontend && npm run build",
        "start": "cd backend && npm start"
    }
}