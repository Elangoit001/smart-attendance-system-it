{
    "name": "smart-dept-system-root",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install --prefix backend && npm install --prefix frontend",
        "build": "npm run install-all && cd frontend && ../node_modules/.bin/vite build",
        "start": "node backend/server.js"
    },
    "dependencies": {
        "vite": "^5.1.4",
        "@vitejs/plugin-react": "^4.2.1"
    }
}